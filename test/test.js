var assert = require('assert');
//var expect = require('chai').expect;
var should = require('chai').should();
//import Fn from './../src/data/Fn.js';


describe('Fn', () => {
  /*
  beforeEach('Setting up the userList', function(){
    console.log('beforeEach');
    loginController.loadUserList(['abc123','xyz321']);
  });
  */

  describe('constructor', () => {
    it('should create an instance of the Fn class', () => {
      /*let abc = new Fn();
      abc.should.be.an.instanceOf(Fn);
      abc.should.have.property('_f');*/
      let fds = true;
      fds.should.equal(true);
    });
  })

  describe('FnF', () => {

  })
});
